<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.melon.dao.like.ILikeDao">

    <!--    좋아요 이력 추가-->
    <insert id="insertLikeLog" parameterType="com.melon.dto.like.LikeDto">
        INSERT INTO
            USER_LIKE (LIKE_CNT, MEMBER_ID, ARTIST_ID)
        VALUES
            (like_cnt_seq.nextval, 'admin', #{artistId})
    </insert>

    <!--좋아요 이력 삭제-->
    <delete id="deleteLikeLog" parameterType="com.melon.dto.like.LikeDto">
        DELETE
        FROM
            USER_LIKE
        WHERE
            MEMBER_ID = 'admin' AND ARTIST_ID = #{artistId}
    </delete>


    <!-- 가수 좋아요 했는지 조회-->
    <select id="isUserLikedArtist" resultType="int" parameterType="com.melon.dto.like.LikeDto">
        SELECT
            COUNT(*)
        FROM
            USER_LIKE
        WHERE
            MEMBER_ID = 'admin'
          AND
            ARTIST_ID = #{artistId}
    </select>

</mapper>